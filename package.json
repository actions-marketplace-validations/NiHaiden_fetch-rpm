{
  "name": "fetch-rpm",
  "version": "1.0.0",
  "description": "GitHub Action to fetch architecture-matching RPM assets from release pages",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test"
  },
  "keywords": [
    "github-action",
    "rpm",
    "release"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
